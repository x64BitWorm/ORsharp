# Пример логического И с использованием ленивых параметров 
Тут следует заметить что если первый параметр И равен нулю, то второй не будет расчитан
#,

and = (x, y -> 
	if (bigger x 0) (
		if (bigger y 0) (
			1
		) (
			0
		)
	) (
		0
	)
),

f1 = (x -> print "called 1", x),
f2 = (x -> print "called 2", x),

and (f1 0) (f2 1)
