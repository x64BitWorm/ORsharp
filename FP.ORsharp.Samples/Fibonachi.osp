# Фибоначчи
Локальная функция fibP служит для получения X члена с конца.
Обратим внимание что функция специально написана неоптимизированно
(требует построения дерева, вместо линейной сложности), поэтому не рекомендуется использовать ее для
расчета больших чисел.
#,

fib = (n ->
	fibP = (x -> fib (minus n x)),	
	if (bigger 3 n) (
		1
	) (
		plus (fibP 1) (fibP 2)
	)
),

res <- fib 10,
print res,
0
